# Chameleon Morphing Package

This package provides low-overhead packet shaping primitives for research prototypes.

- `PaddingConfig`: controls randomized payload length normalization.
- `JitterConfig`: controls delay injection.

The implementation intentionally stays small and idiomatic for Go 1.22+.
